services:
  - type: web
    name: smart-attendance
    env: docker
    plan: starter
    autoDeploy: true
    healthCheckPath: /health
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
